[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "notilify"
version = "0.1.0"
description = "Official Python SDK for the Notilify transactional SMS API"
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [{ name = "Notilify", email = "support@notilify.com" }]
keywords = ["notilify", "sms", "transactional-sms"]

[project.urls]
Homepage = "https://notilify.com"
Repository = "https://github.com/codelinglabs/notilify-sdks"
Issues = "https://github.com/codelinglabs/notilify-sdks/issues"

[tool.setuptools.packages.find]
where = ["src"]
